<Window x:Class="CourseSelection.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ders Seçimi"
        Height="600"
        Width="900"
        WindowStartupLocation="CenterScreen"
        Background="White">

    <DockPanel>
        <!-- Üstte Navigation Bar -->
        <StackPanel Orientation="Horizontal"
                    DockPanel.Dock="Top"
                    Background="#4CAF50"
                    Height="50">
            <Button Content="Ders Seçimi"
                    Width="120"
                    Margin="10"
                    Click="DersSecimiButton_Click"
                    Foreground="White"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    FontWeight="Bold" />

            <!-- Diğer butonlar (profil, çıkış vb.) buraya eklenebilir -->
        </StackPanel>

        <!-- Ortada sayfaların yükleneceği Frame -->
        <Frame x:Name="MainFrame"
               NavigationUIVisibility="Hidden" />
    </DockPanel>
</Window>
